

## Intento de configuración de Tool Calling Force Mode (27/02/2026 - 14:43)

### Solicitud del usuario
- Usuario pidió: "si configura force pero dime donde lo haces"
- Objetivo: Configurar tool calling a modo `force` y explicar ubicación/configuración

### Descubrimiento técnico importante
- **`toolCalling` NO es un parámetro configurable directamente** en OpenClaw
- **Intento fallido:** `openclaw config set agents.defaults.toolCalling "force"`
- **Error:** "Config validation failed: agents.defaults: Unrecognized key: 'toolCalling'"
- **Conclusión:** Tool calling es controlado por el modelo (DeepSeek Chat), no por configuración global

### Configuración actual verificada
- **Archivo:** `~/.openclaw/openclaw.json`
- **Sección `agents.defaults`:** No contiene clave `toolCalling`
- **Modelo actual:** DeepSeek Chat (tool calling integrado en modelo)
- **Modo actual:** `auto` (inteligente, decidido por modelo)

### Alternativas para "forzar" tool calling
1. **Crear agente personalizado** con prompt que instruya uso forzado de herramientas
2. **Modificar prompt del agente main** para incluir instrucciones específicas
3. **Usar skill específico** que active comportamiento de tool calling forzado
4. **Configurar modelo diferente** con comportamiento de tool calling más agresivo

### Ejemplo de prompt para forzar tool calling
```
Eres un asistente que SIEMPRE debe usar herramientas cuando están disponibles.
Nunca respondas sin usar herramientas si hay una herramienta relevante.
Herramientas disponibles: exec, read, write, message, etc.
```

### Demostración de tool calling funcionando (modo `auto`)
- **Comando ejecutado:** `date` via herramienta `exec`
- **Resultado:** "Fri Feb 27 11:08:15 CET 2026"
- **Evidencia:** Tool calling YA funciona, pero de forma inteligente (no forzada)

### Opciones presentadas al usuario
1. Crear agente personalizado con tool calling forzado
2. Modificar prompt del agente main
3. Usar skill específico para modo herramienta
4. Configurar agente alternativo con comportamiento diferente

### Pre-compaction memory flush
- Sexta compactación detectada en el día
- Almacenando descubrimiento sobre limitaciones de configuración de tool calling
- Contexto actual: Tool calling no es parámetro configurable directamente en OpenClaw, se necesitan alternativas
- Tareas pendientes: Implementar alternativa seleccionada por usuario para simular `force` mode